media:
  input: assets/images
  output: /assets/images

content:
  - name: posts
    label: Blog Posts
    type: collection
    path: _posts
    format: frontmatter
    fields:
      - name: title
        label: Title
        type: string
      - name: cover_image
        label: Cover Image
        type: image
      - name: body
        label: Content
        type: rich-text